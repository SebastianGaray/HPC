// C includes
#include <stdlib.h>

// User includes
#include "files.h"


void readFile(char *inputFile, int N){
    float *array = (float*)malloc(sizeof(float)*N);
    int i = 0;
    int value;
    while (i!=N){
        fread();
    }
}