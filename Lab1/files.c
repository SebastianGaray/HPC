// C includes
#include <stdlib.h>

// User includes
#include "files.h"


void readFile(char *inputFile, int N){
    float *values = (float*)malloc(sizeof(float)*N);
    int i = 0;
    while (i!=N){

    }

}